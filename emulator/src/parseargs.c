#include <stddef.h>

#include "parseargs.h"

bool Hbc56EmulatorArgs_Parse(int argc, char* argv[], const Hbc56EmulatorArgs* args) {
    if (argv != NULL) {
        return true;
    }
    return false;
}